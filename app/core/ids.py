def station_id(provider_id: str, provider_station_id: str) -> str:
    return f"{provider_id}-{provider_station_id}"


def reach_id(provider_id: str, provider_reach_id: str) -> str:
    return f"{provider_id}-{provider_reach_id}"
